--help flag should work on each command

- allow filtering on start/finish and key/value pairs in export
- simplify parsing of key values pairs so that they don't need quotes around them.

List command
------------

Implement after and before